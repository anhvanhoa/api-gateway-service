run:
	go run src/main.go

generate:
	@echo "🚀 Chạy tool generate..."
	go run tools/generate.go

build:
	export NODE_ENV=production && go build -o api-gateway.exe src/main.go

start:
	./api-gateway.exe

clean:
	rm -f api-gateway.exe

docker-build: ## Build the application and run it in the container
	export CONFIG_FILE=${CONFIG_FILE} && docker compose up -d --build